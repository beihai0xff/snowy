# Snowy Docker Compose optional switches.
#
# Model base_url/model/api_key are not configured here.
# Copy configs/config.example.yaml to configs/config.yaml and edit llm.models[].

# SNOWY_SERVER_RUN_MODE=all
