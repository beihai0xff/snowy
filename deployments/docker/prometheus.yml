# Snowy — Prometheus 配置
# 参考技术方案 §20 可观测性设计

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Prometheus 自身
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Snowy 单体服务 (Go + Gin + Prometheus exporter)
  - job_name: "snowy"
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8080"]
        labels:
          service: "snowy"

  # Redis Exporter（如需独立部署可取消注释）
  # - job_name: "redis"
  #   static_configs:
  #     - targets: ["redis-exporter:9121"]

  # MySQL Exporter（如需独立部署可取消注释）
  # - job_name: "mysql"
  #   static_configs:
  #     - targets: ["mysql-exporter:9104"]

  # OpenSearch（通过 REST API 采集）
  # - job_name: "opensearch"
  #   metrics_path: /_prometheus/metrics
  #   static_configs:
  #     - targets: ["opensearch:9200"]
